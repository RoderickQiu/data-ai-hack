# Slack surface only. Install into the same venv as the repo root requirements.
#   pip install -r slack/requirements.txt
# Pin exactly with `pip freeze` once it runs, before the first Snyk scan.

slack-bolt>=1.21
fastapi>=0.115
uvicorn>=0.32
pydantic>=2.9
python-dotenv>=1.0
reportlab>=4.0
